<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.go.meti.drone.relatedpartiesnotice.messagesend.repository.mapper.AirwayReserveRelatedMapper">
  <insert id="insertAirwayReserveRelated" parameterType="jp.go.meti.drone.relatedpartiesnotice.messagesend.repository.entity.AirwayReserveRelatedEntity">
	INSERT INTO airway_reserve_related (
		airway_reserve_id,
		request_id,
		operator_id,
		operator_name,
		related_category,
		notification_email,
		notification_phone,
		notification_remark,
		creation_id, 
		creation_datetime, 
		update_id, 
		update_datetime) 
		VALUES (
			#{airwayReserveId},
			#{requestId},
			#{operatorId},
			#{operatorName},
			#{relatedCategory},
			#{notificationEmail},
			#{notificationPhone},
			#{notificationRemark},
			#{creationId},
			CURRENT_TIMESTAMP,
			#{updateId},
			CURRENT_TIMESTAMP)
  </insert>
</mapper>